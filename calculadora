from math import *
op = input("Type of operation: ")
n1 = float(input("First number: "))
if op == "sqrt":
    r = sqrt(n1)
else:
    n2 = float(input("Second number: "))
    if op == "+":
        r = n1 + n2
    elif op == "-":
        r = n1 - n2
    elif op == "/":
        r = n1 / n2
    elif op == "*":
        r = n1 * n2
    elif op == "^":
        r = pow(n1, n2)
print(r)
